domain: select
service: select_last
name: Last
description: Selects the last option.
fields: {}
target:
  entity:
  - domain:
    - select
handler: builtin:select.select_last
supports_response: false
