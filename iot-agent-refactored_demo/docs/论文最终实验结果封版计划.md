# 论文最终实验结果封版计划

> 计划版本：v1.15
>
> 建立日期：2026-07-25
>
> 目的：把当前“可运行的实验原型 / 开发态结果”推进到可以支撑论文最终实验章节的、可复现、可审计、可解释的正式结果。
>
> 本文档是执行计划，不是实验方案本身。后续如果代码、场景、指标或环境发生变化，应递增计划版本，在文末记录变更原因；不得静默修改封版口径。

## 一、封版目标

最终封版至少需要同时满足以下条件：

1. 36 个场景的脚本、ground truth、指标映射和 RQ 标签完全同步，且每个场景都能在正式 runner 中被执行和判定。
2. Ours、B0-B5、全部预注册 ablation 使用相同的 world、场景、seed 规则和评测口径；Oracle 与 Agent 的适用范围明确，不混淆汇报。
3. 每个正式配置完成预注册数量的独立 seed 运行：Oracle 主结果使用 `N=30`，Agent / 次要结果使用 `N=20`；如实际无法达到，必须在论文中明确降级为开发态或补充实验。
4. 任务成功判定同时检查动作语义、澄清行为、记忆状态断言和最终设备状态，不能出现“trace 失败但 TSR=1”的矛盾。
5. 主要指标、次要指标、统计检验、置信区间和成本口径固定，并由脚本自动生成，禁止手工挑选结果。
6. 所有正式结果均能追溯到：代码提交、配置、world 版本、scenario 版本、system policy、seed、原始 trace 和生成脚本。
7. 只有通过全部封版门槛的结果才能进入论文主表；未通过的结果只能放在开发记录、消融探索或局限性部分。

## 二、当前基线与硬问题

当前仓库已经有记忆服务、冻结 world、36 个场景、baseline / ablation、trace、报告和 `67/67` smoke tests，并且正式结果已经在独立根目录 `experiments/results/formal` 中完成 full formal run。当前必须优先处理的事实如下：

- formal 根已完成预注册 seed 数：Oracle `30`、Agent `20`、Baseline `30`、Ablation `20`，并重建了表格、图表、统计摘要、显著性摘要、实验摘要和运行索引。
- 36 个场景中目前是 31 个 Oracle 场景和 5 个 Agent 场景；这两个集合仍需在主表中保持清晰边界，不可混淆汇报。
- Ours、baseline、ablation 已经产生可复算的正式结果，但仍需在最终封版前做 trace / manifest / 统计一致性复核。
- Agent planner 仍包含启发式 fallback，正式论文表述必须保持其作为受控实验路径或次要实验路径的定位。
- 截至 Saturday, July 25, 2026，真实 external-LLM agent 路径已在独立目录 `experiments/results/agent_llm_smoke` 扩到 `real_llm_candidate_20260725_two_seed`：`G1 / E1 / B6 / E2 / E3` 共 5 个 agent 场景、2 个固定 seeds、10/10 成功 trace，且成功 trace 全部 `agent_backend=external_llm`。
- 这批 two-seed real-LLM candidate 结果已具备独立 manifest、raw trace、usage metadata、model/provider、backend 审计字段，以及 consolidated `audit.json / comparison.json / seal_readiness_audit.json`；同时保留了 `B6@1002` 的首次失败尝试。
- 这批 two-seed real-LLM candidate 仍不能覆盖 `formal_v2`，也不能写成论文最终封版结果；当前硬缺口仍是 `seed_count=2 < 20` 与来源 run 混合 revision。
- `strict_oracle_ablations_20260726_v1` 已经完成 `8 × 36 × 20 = 5760` 个 Oracle 消融 unit，并通过 strict audit；这一部分不再是本轮缺口。
- `strict_agent_pilot_20260726_v2` 当前只覆盖 `10 / 7560` 个真实 Agent unit，但已经在单一 clean revision `0f20eab` 上确认 `external_llm / no-fallback / no-mixed-revision` 审计链可用，并给出 `9828` 次调用、约 `61411392` total tokens 的 full-grid 外推。
- 统计和报告链已能从 formal 根目录重建产物，但最终封版前仍要做一次干净目录的一致性审计。

当前结果已经进入“正式结果候选封版”阶段，不再是单纯的开发态输出。

## 三、执行总顺序

必须按以下依赖顺序推进。后一个阶段不得绕过前一个阶段的失败门槛：

`P0 口径冻结` → `P1 判定与指标修正` → `P2 场景与覆盖封闭` → `P3 系统与对照有效性` → `P4 正式多 seed 运行` → `P5 统计与结果审计` → `P6 可复现封版` → `P7 论文写入`

## 四、阶段计划与验收门槛

### P0：冻结实验口径

目标：先把“什么算成功、每个指标怎么算、哪些结果可进论文”写死。

任务：

- 对照《实验方案-v3(1)-中文翻译.md》，建立 36 场景的 `scenario_id → category → RQ → primary/secondary metric` 矩阵。
- 明确 Oracle 和 Agent 的实验角色：Oracle 用于机制与记忆闭环控制实验，Agent 用于端到端可行性与安全路径；不能把两者无说明地合并。
- 明确主指标、次指标、成本指标和统计单位。建议主指标至少包括 TSR、WDR、CB、PM、SRR / 相关 RQ 指标；其余作为次指标。
- 明确重复单位是 seed、scenario 还是 task instance，明确跨 seed 和跨场景的聚合方式。
- 把所有“开发态”“探索性”“正式结果”的目录和命名规则写入配置与文档。

验收证据：

- 一份可审计的覆盖矩阵。
- 一份固定的指标定义与公式文档或代码注释。
- 代码配置中没有隐式改变正式实验口径的默认值。

封版门槛：没有口径冻结，不进入代码大改和正式运行。

### P1：修正任务成功判定与指标一致性

目标：消除“动作失败但 TSR 成功”的核心错误。

任务：

- 将动作执行结果分为至少：`action_success`、`clarification_success`、`memory_assertion_success`、`final_state_success`、`task_success`。
- `planner.select` 不能被当作真实 Home Assistant service；如果场景要求 `light.turn_on`，必须检查最终执行的 service、entity 和 args。
- 对复合任务逐步检查动作序列、前置条件、后置状态和最终状态。
- 对 `expect_action`、`expect_no_action`、`expect_memory`、`expect_final_state` 的失败统一写入结构化 assertion result，不只写字符串。
- 重新定义 TSR：只在任务要求的动作 / 澄清 / 记忆状态 / 最终状态全部满足时计为成功；不能仅比较两个可能为空或被错误覆盖的状态字典。
- 增加失败分类：错误设备、错误 service、未澄清、错误记忆生命周期、world 执行失败、断言失败、Agent 输出不可解析。

验收证据：

- 原先失败的 A1 等场景在 trace 中能清楚显示失败原因；修正后成功任务的 `outcome`、`TSR` 和 manifest `failed_task_ids` 一致。
- 构造至少 5 个反例测试：最终状态相同但 service 错误、动作正确但状态错误、应澄清却直接执行、应静默却产生动作、记忆断言失败。
- `failed_task_ids` 数量与任务成功率逐项可复算。

封版门槛：任何正式 run 出现 `outcome=failure` 但 TSR 仍计为成功的情况，都必须阻止封版。

### P2：补齐 36 场景和 ground truth

目标：让测试集从“有 36 个文件”变成“36 个完全指定、可解释、可复现的正式任务”。

任务：

- 逐场景核对时间、初始 world、初始 memory、用户话语、memory ops、预期检索、预期澄清、动作、最终状态和失败注入。
- 给每个场景补齐唯一且非空的 ground truth，避免通过空字典或默认状态获得虚假的成功。
- 确认 A-H 类场景分别覆盖 RQ1-RQ5，且同一 RQ 至少有正向和负向路径。
- 确认 F7 delete / privacy、F6 成功 merge、G5 瞬时状态不持久化、H1/H2 查询阈值等新增主张都有可执行断言。
- 确认双标注文件不是空占位；如果采用专家标注，记录标注规则和一致性计算；如果全部是确定性脚本，明确说明不把占位标注冒充人工标注。
- 为每个场景增加 `expected_outcome_class` 和覆盖说明，方便报告按场景审计。

验收证据：

- 36 个场景全部通过 schema 校验。
- 36 个场景全部能运行，不允许因缺少 fixture、不可执行 action 或空 ground truth 被跳过。
- 场景覆盖矩阵显示每个主要 RQ、主要指标和关键设计主张至少被一个正式场景覆盖。
- 每个场景至少有一条正向或负向可验证断言，不接受只有 `say` 没有可验证结果的场景。

封版门槛：如果某场景只能依赖 runner 特判或人工解释才能判定，不进入论文主实验。

### P3：确认 Ours、baseline、ablation 真正有效

目标：确保比较的是不同机制，而不是同一套启发式代码改了几个配置名。

任务：

- 审计 `SystemConfig` 每个开关是否真实改变执行语义，并为每个开关指定至少一个敏感场景。
- 确认 B0-B5 的行为边界与方案一致，尤其是 B4 大上下文和 B5 Generative Agents 类比基线。
- 对每个 ablation 建立机制→场景→预期变化→指标的映射，不能接受所有结果完全相同而没有解释。
- Agent 路径必须记录原始模型输出、解析结果、工具调用、world 反馈和失败原因；启发式 fallback 只能作为单独的 fallback 实验，不能伪装成正式 Agent 结果。
- 如果本阶段仍以 Oracle 为主，论文中必须明确 Oracle 是受控 planner，不将其描述为完整真实 Agent。

验收证据：

- 每个 baseline / ablation 至少有一个回归测试证明其开关被触发。
- 每个预注册机制至少有一个场景能观察到预期方向的行为差异，或者有正式的“该机制在此测试集上不敏感”解释。
- Agent 正式路径不依赖未记录的 fallback。

封版门槛：无法区分的 baseline / ablation 不进入最终比较表，只能保留在开发记录。

### P4：执行正式多 seed 实验

目标：按预注册协议获得真正的重复实验数据。

任务：

- 在确认环境、依赖、数据库检索后端、world 和代码提交后，执行 Oracle `N=30` 协议重复；这些重复用于复现审计，统计推断单位固定为 scenario，不把确定性 seed 伪装成独立样本。
- 执行 Agent / secondary 结果 `N=20`，并明确它是次要实验还是独立主实验。
- 每个 system、planner mode、scenario、seed 都写入独立 trace；不得因重复运行覆盖 trace。
- 固定随机种子来源、模型版本、采样温度、重试策略和失败处理策略。
- 运行前清理或隔离旧开发态结果，正式结果使用新 run id，不把旧文件混入聚合目录。
- 正式运行过程中不根据中间结果修改场景、指标或系统配置；如必须修改，递增实验版本并重新开始正式运行。

验收证据：

- 每个正式 run 的 manifest 显示正确 seed 数、scenario 数和系统配置。
- 每个正式配置的 `metrics.by_seed.json` 数量符合协议：Oracle 30，Agent 20。
- 所有 seed 都有 trace，失败任务也必须有完整 trace，不能静默丢弃。
- 运行结果绑定 git revision、配置快照、world version、scenario version 和 system policy version。

封版门槛：任一主要配置仍是 `count=1` 或缺 seed，不能进入论文主表。

### P5：统计与结果审计

目标：让结果能够被复算、解释和诚实地汇报。

任务：

- 优先按预注册单位做 paired comparison；Oracle 按同一 scenario、不同 system 配对，真实随机 Agent 才按同一 seed 配对。
- 选择适合指标分布的统计检验，不把 `paired_count=1` 的结果当作显著性证据。
- 输出均值、标准差 / 标准误、95% CI、效应量、校正后的 p 值，并记录检验方法和多重比较校正方法。
- 对二元指标和比例指标检查是否需要 bootstrap、置换检验或分层聚合，避免直接套用不适合的 Cohen's d。
- 报告 overall、按 category、按 RQ、按 scenario 的结果，避免少数容易场景掩盖失败场景。
- 审计主要异常：TSR 与 outcome 不一致、空 ground truth、零分母、指标全为 0、某配置完全相同、失败任务缺 trace。
- 重新生成表格、图、Markdown 报告和 significance summary；报告中明确开发态结果与正式结果的边界。

验收证据：

- 任意论文表格中的数字都能从原始 trace 重新生成。
- 统计报告列出样本量和有效样本量，不隐藏失败或缺失任务。
- 主要结论在不同聚合方式下不会因一个场景或一个 seed 而完全反转；若会，必须作为限制报告。

封版门槛：统计脚本无法从干净结果目录重建论文表格时，不封版。

### P6：可复现性与最终封版

目标：冻结一份第三方可以检查的最终实验工件。

任务：

- 新建正式结果 run id，隔离所有 `dev`、`check`、`test` 结果。
- 保存最终配置、依赖版本、Python 版本、数据库检索后端、模型信息（若使用）、git commit、运行命令和开始结束时间。
- 从干净结果目录重新执行报告生成链，验证不会依赖旧的聚合文件或本地缓存。
- 运行完整 smoke、场景 schema 校验、trace schema 校验、结果一致性审计和统计重建。
- 检查工作区状态；代码、场景、指标、文档和生成脚本在同一组提交中提交。
- 在 `docs/实验结果摘要.md` 标注正式 run id、样本量、最终指标和限制；在 `docs/实验实现进展.md` 标记完成度，但不能夸大结论。

最终封版证据包至少包括：

- 正式配置文件和版本快照。
- 36 场景及 ground truth / 标注资产。
- 每个正式配置的 manifest。
- 原始 trace 和 maintenance trace。
- 聚合指标、per-scenario 指标、seed 级指标。
- 论文表格、图、统计检验摘要。
- 运行日志、依赖信息和 git revision。
- 最终限制说明和失败样本审计表。

封版门槛：任何结果如果无法从 trace 和配置逐项追溯，就不能作为最终结果提交论文。

### P7：写入论文

只有 P0-P6 全部通过后，才进入论文实验章节。建议写作顺序：

1. 实验设置：world、场景、RQ、baseline、ablation、seed 和指标。
2. 实现细节：memory schema、生命周期、置信度、检索门控、反馈和治理。
3. 主结果：按预注册主要指标展示 Ours 与基线，并报告样本量和不确定性。
4. 消融结果：只解释已通过机制敏感性审计的 ablation。
5. 分析结果：按 RQ、category、场景类型和失败类别分析。
6. 成本与安全：上下文成本、澄清负担、安全门控和失败规避。
7. 限制：仿真 world、Oracle / Agent 差异、样本来源、外部依赖和未覆盖能力。

论文中必须明确区分：

- 正式结果：满足 P0-P6 的 run。
- 开发态结果：用于调试和回归，不参与主结论。
- 探索性结果：未预注册或样本量不足，只用于提出后续假设。

## 五、建议的 Goal 模式拆分

后续每次 Goal 不要同时承担所有阶段，建议按以下目标逐个完成：

### Goal 1：修复成功判定与指标一致性

完成 P1；优先修正 action 语义、assertion 结果、TSR 和失败分类；通过反例测试后再继续。

### Goal 2：完成 36 场景与覆盖矩阵审计

完成 P0 和 P2；输出场景审计清单，补齐空 ground truth、缺失断言和 RQ 映射。

### Goal 3：完成 baseline / ablation / Agent 路径审计

完成 P3；确认每个系统配置真的改变行为，并明确 Oracle、Agent 和 fallback 的论文身份。

### Goal 4：重建正式运行入口和结果隔离

完成 P4 的运行准备，不急于长时间运行；先验证 seed 数、目录隔离、manifest 和断点失败行为。

### Goal 5：执行正式多 seed 运行

完成 P4；只使用已经冻结的代码、配置和场景，不在运行中临时改口径。

### Goal 6：统计、审计和正式报告

完成 P5-P6；重建论文表格、图、统计摘要和最终文档，形成封版证据包。

### Goal 7：论文结果写入与最终复核

完成 P7；逐项核对论文数字与正式结果文件，完成最后一次一致性检查和提交。

## 六、明确的停止条件

遇到以下情况，应暂停正式运行并先修复，而不是继续堆数据：

- outcome、TSR、action assertion 和 final state 不一致。
- 正式结果混入 dev / test / check 目录。
- seed 数量不足或不同系统使用了不一致的 seed。
- 场景依赖 runner 中未在方案中声明的特判。
- Agent 结果实际来自 fallback，但报告将其标记为正式 Agent。
- 统计脚本读到了旧结果、覆盖结果或缺失结果。
- 为了得到更好数字而临时修改场景、指标或失败处理规则。

## 七、计划变更记录
| 版本 | 日期 | 变更内容 | 原因 | 影响 |
| --- | --- | --- | --- | --- |
| v1.7 | 2026-07-25 | 补记真实 external-LLM Agent 已扩到 two-seed candidate：5 个 agent 场景、2 个固定 seeds、10/10 成功 trace，并新增 consolidated audit/comparison；同时记录 `B6@1002` 的首次失败尝试 | 真实 LLM 路径已从单-seed smoke 前进到可比较的 two-seed candidate，需要把成本、限制和后续扩量门槛写清楚 | 后续 Goal 应直接基于 `real_llm_candidate_20260725_two_seed` 扩到 `N=20`，而不是再手工拼单场景 manifest |
| v1.8 | 2026-07-26 | 新增严格主实验矩阵、36 场景双 planner 覆盖、单 unit 串行 runner、strict audit 与成本估算脚本；`64/64` smoke 全部通过 | 目标已从“证明 five-scenario candidate 可行”推进到“为严格 full-grid 主实验做自动化准备”，需要把工具链和边界写清楚 | 后续 paid pilot 必须使用新的 strict serial runner 与独立结果根，在 clean revision 上逐 unit 执行，不再手工拼接 run |
| v1.9 | 2026-07-26 | 在 clean revision `0f20eab` 上启动 `strict_agent_pilot_20260726_v2`：完成 `Ours` 的 `A1 / C1 / H2 / B6 @ 1001`，以及 `B6 @ 1001` 的 `Ours + B0-B5` paired pilot；累计 `13` 次调用、`81232` total tokens，并生成 strict audit 与 full-grid cost estimate | 仅有基础设施并不足以评估真实 LLM 行为与成本，需要少量真实调用验证 external_llm 路径、观测边界失败，并为 full-grid 估算提供样本 | 后续下一轮应继续补代表场景 paired pilot，或在接受当前成本估算后按分批清单启动 full-grid；当前仍不能宣称真实 LLM 主实验完成 |
| v1.10 | 2026-07-26 | 补记 `run_strict_group.py`、prompt/completion token 归一化审计，以及 `strict_oracle_ablations_20260726_v1` 已完成 `5760/5760` unit、strict audit `pass` | strict 工具链已从“单 unit 与 pilot 可跑”推进到“Oracle 消融全覆盖可批量执行且 token 统计口径稳定”，需要把零成本已完成部分与剩余真实 LLM 缺口分开记录 | 后续 paid pilot 的唯一核心缺口已集中到真实 `Ours + B0-B5` 主实验；若代码再变动，必须重新提交并在新 revision 的独立 pilot 目录继续 |
| v1.11 | 2026-07-26 | 在 clean revision `fbedd87` 上启动 `strict_agent_pilot_20260726_v3`：完成 `G1 / D3 / E1 / F2 / C1 @ 1001`，累计 `9` 次调用、`11635` total tokens；其中 `G1/D3/E1/F2` 成功，`C1` 因漏掉有效窗口内动作而失败 | 需要在新的 clean revision 上继续补足拒答、更新、routine、冲突、过期等代表场景，并验证 `v2` 之外的真实行为样本 | 后续下一轮应优先在 `fbedd87` 上补第二个 `Ours + B0-B5` paired pilot 场景；当前 full-grid 成本对样本敏感性仍然很高，不能直接发起正式全量运行 |
| v1.12 | 2026-07-26 | 将 strict 工具链继续收紧到正式全量 run 可用：`TraceWriter` 改为原子落盘，`run_strict_serial_unit.py` 的 `--resume` 改为校验 `trace + maintenance + manifest` 三件套完整性后再决定跳过，`run_strict_group.py` 升级为 bounded-concurrency 协调器，支持阶段性 summary、预算统计、技术故障有限重试与自动降并发，`audit_strict_experiment.py` 新增 maintenance trace 完整性审计，smoke 提升到 `68/68` | 真实 real-LLM 全量 run 的主要风险已从“脚本能否调用”转向“长时间 paid run 中断后是否会误跳过半残结果、并发时是否会留下不可审计产物”，因此需要先补 execution safety | 下一轮可以直接从当前代码状态启动 `seed=1001 → seed=1002 → seeds=1003-1030` 的 staged full-grid real-LLM 主实验；若代码再改，仍必须先提交并在新 revision 与新结果根下重启正式 run |
| v1.13 | 2026-07-26 | 做实 request-level seed 协议：`seed` 已从 runner 传到 external LLM client，并写入 trace/manifest 审计字段；新增 `probe_external_llm_seed_support.py`，当前真实探测结果确认 `newapi / gpt-5.4-mini-2026-03-17 / http` 支持 request-level `seed`，smoke 提升到 `69/69` | 正式 full-grid run 前必须先冻结“seed 只是 replicate id”还是“provider 真实接收 seed”这一口径，否则后续文档和统计表述会前后矛盾 | 后续 staged full-grid real-LLM 主实验应统一按 `provider_seed` 协议执行，并在最终报告中明确记录 requested seed、provider/model、temperature=0.0 与服务端返回信息 |
| v1.14 | 2026-07-26 | 在 `strict_main_agent_final_20260726_v1` 的第一次阶段 A 正式尝试中，发现 `F1` 这类无 `say` 步的 agent 场景会因 `agent_backend=null` 被 strict check 误判；已修复为保留 `agent_backend=external_llm` 并标记 `agent_seed_protocol=no_agent_call_required`，smoke 提升到 `70/70` | 真实主实验第一次正式扩量暴露了“零 planner 调用但仍属于 agent 配置路径”的协议缺口；如果不先修，full-grid run 会在同类场景稳定中断 | `strict_main_agent_final_20260726_v1` 只能作为作废 partial run 保留，不得继续扩量或混入最终数据；后续必须在新 revision 和新的 `strict_main_agent_final_20260726_v2` 结果根下重新开始阶段 A |
| v1.15 | 2026-07-26 | 在 `strict_main_agent_final_20260726_v2` 的第二次阶段 A 正式尝试中，成功越过 `F1` 阻断点并推进到 `35/252`，随后在 `H2` 暴露出新的执行层缺口：模型给出缺少必需参数的动作时，runner 会直接抛 Python 异常；现已修复为把无效 service/args 记成失败动作记录和行为失败，smoke 提升到 `71/71` | 真实主实验第二次正式扩量证明当前剩余风险已从“场景协议缺口”收缩到“模型坏动作的执行层容错”；如果不修，正式 run 仍会被少量坏动作稳定打断 | `strict_main_agent_final_20260726_v2` 只能作为作废 partial run 保留，不得继续扩量或混入最终数据；后续必须在修复后的新 revision 与新的 `strict_main_agent_final_20260726_v3` 结果根下重新开始阶段 A |
| v1.6 | 2026-07-25 | 补记真实 external-LLM Agent 已完成最小 smoke 与 5 场景单-seed candidate 验证，并明确其与 formal_v2 heuristic fallback 隔离 | 代码与结果状态已发生实质变化，需要避免论文口径继续把 fallback 与真实 LLM 混写 | 后续 Agent 结果扩量应基于 `agent_llm_smoke` 独立目录继续推进 |
| v1.5 | 2026-07-25 | 在隔离根完成 formal_v2 全量运行与自动化审计：17 manifests、12,500 traces、5张非空表、5份图数据，全部绑定 `2aed34c`；审计 pass | formal_v2 代码口径冻结并完成重跑 | Oracle confirmatory 结果可以进入论文主表；fallback Agent、人工标注 pending 与不敏感消融必须作为限制披露 |
| v1.4 | 2026-07-25 | 废止旧 formal 的伪独立 seed 统计口径，增加 State TSR、场景级配对统计、B4 全量历史、Agent fallback 降级、人工标注 pending 与 formal_v2 审计门槛；明确数据库检索即正式方案 | 对旧 formal 进行论文可用性复核后发现空主表、TSR/final-state、统计单位和 Agent 硬编码问题 | 必须使用新代码在隔离根重跑 formal_v2；旧 formal 仅保留历史，不进入最终主表 |
| v1.3 | 2026-07-25 | 完成 formal 独立结果根目录与 full formal run，正式结果达到 Oracle 30 / Agent 20 / Baseline 30 / Ablation 20 seeds，并重建全部表图摘要索引 | 将开发态结果推进到可封版的正式结果候选状态 | 进入最终一致性审计与封版提交阶段 |
| v1.2 | 2026-07-25 | `C3` 软过期层级对齐为 `dormant`，并补记当前 smoke 全绿状态 | 修正场景标注与生命周期实现的层级口径冲突 | 现有薄场景回归全部通过，计划继续推进多 seed 与正式结果封版 |
| v1.1 | 2026-07-25 | 补充任务级结构化 assertion 结果、`task_success` 口径，以及 query / automation / control 的任务分流和 `valid_from` 门控 | 修复“最终状态正确但动作语义失败”的指标脱节问题，并继续收紧时间窗与任务类型语义 | 当前 smoke 仍有少量场景未完全收口，需继续做场景与 planner 校准 |
| v1.0 | 2026-07-25 | 初始建立封版计划，增加 P0-P7 阶段、验收门槛和 Goal 拆分 | 当前开发态结果已具备实验骨架，但不满足正式论文结果要求 | 后续 Goal 模式按阶段推进，不再以 smoke 全绿作为封版依据 |

## 八、当前建议的下一步

formal_v2 自动化封版已经完成。论文写作可使用 Oracle confirmatory 主表与场景级 paired exact sign test；不得把 `heuristic_fallback` Agent 写成真实 LLM 结果，不得报告不存在的人工 κ，也不得声称 `-AsymFeedback` / `-FeatureAbsorption` 已表现出边际收益。截止 Sunday, July 26, 2026，`strict_oracle_ablations_20260726_v1` 已经完成 `5760/5760` unit 并通过 strict audit，strict serial pilot 也已经分别在 clean revisions `0f20eab` 与 `fbedd87` 上累计拿到 `15` 个真实 `external_llm` unit、`22` 次 API 调用、`92867` total tokens，并观测到 `H2` 与 `C1` 两类有效模型行为失败；其中 `C1` 的失败是“有效窗口内漏执行”，不是“过期后误执行”。当前 strict runner 已进一步补齐原子落盘、三件套 resume 校验、bounded concurrency、预算统计与 maintenance trace 审计，并已通过真实 probe 确认当前 provider 支持 request-level `seed`，因此后续 full-grid 主实验可以统一按 `provider_seed` 协议执行。与此同时，两次阶段 A 正式扩量都已暴露并推动修复真实执行链缺口：`strict_main_agent_final_20260726_v1` 在 `22/252` 处因 agent no-op 场景的 backend 标记协议缺口作废，`strict_main_agent_final_20260726_v2` 在 `35/252` 处因模型坏动作触发未捕获异常作废；这两批 partial run 只能保留为技术性排障记录，不能混入最终主实验数据。同时，基于 `v2` 与 `v3` 的 full-grid 外推在 `17592120 ~ 61411392` total tokens 之间波动，说明当前成本估计对场景样本仍然敏感。在 full-grid real-LLM 结果满足 seed、revision 与 no-fallback 审计前，它仍只能作为 candidate / exploratory 结果追加，不覆盖 formal_v2。
