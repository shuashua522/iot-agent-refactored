# 实验结果摘要

> 生成日期：Saturday, July 25, 2026

## 1. 当前可引用结果

当前仓库里需要明确区分两条结果线：

- `experiments/results/formal_v2/`
  - 当前论文主表可引用的正式结果根。
  - 自动化工件审计状态为 `pass`。
  - 确认范围为 `oracle_only`。
- `experiments/results/agent_llm_smoke/`
  - 真实 external-LLM Agent 的独立候选结果根。
  - 当前最新 consolidated manifest 为 `experiments/results/agent_llm_smoke/reports/real_llm_candidate_20260725_two_seed/Ours/agent/manifest.json`。
  - 当前结果分类仍是 `real_llm_candidate`，不能覆盖 `formal_v2`。

人工双标注状态仍为 `pending_human_annotation`，Cohen's κ 仍为 `None`。`configured_agent_formal_v2` 仍是 `heuristic_fallback`，不能写成真实 LLM Agent。

## 2. 论文主表参考

| 结果线 | 覆盖范围 | 分类 | 关键指标 | 论文口径 |
| --- | --- | --- | --- | --- |
| `configured_oracle_formal_v2` | 31 个 oracle 场景，30 seeds | `confirmatory` | TSR=`1.0000`，State TSR=`1.0000`，WDR=`0.0000`，CB=`0.4516`，PM=`1.0000` | 可作为论文主表的受控 planner 主结果 |
| `configured_agent_formal_v2` | 5 个 agent 场景，20 seeds | `heuristic_fallback` | TSR=`1.0000`，State TSR=`1.0000`，CB=`1.2000`，UAA=`0.6667`，PM=`1.0000` | 只能作为 fallback / exploratory agent，对外不能声称是真实 LLM |

## 3. 真实 LLM Agent Two-Seed Candidate

| 项目 | 数值 |
| --- | --- |
| consolidated run id | `real_llm_candidate_20260725_two_seed` |
| 场景 | `G1 / E1 / B6 / E2 / E3` |
| seed | `1001 / 1002` |
| 成功 trace | `10/10` |
| backend | 全部 `external_llm` |
| provider / model | `openai` / `gpt-5.4-mini-2026-03-17` |
| 成功 trace API 调用 | `18` |
| 成功 trace token usage | `input 15273 / output 3204 / total 18477` |
| 当前均值指标 | TSR=`1.0000`，State TSR=`1.0000`，CB=`1.2000`，UAA=`0.6667`，PM=`1.0000` |
| 端到端延迟均值 | `11304.41 ms` |

补充审计：

- 本轮 second-seed 成功部分新增 `9` 次调用、`9077` tokens。
- `B6@1002` 首次尝试因过度保守澄清失败，已单独保留失败 trace；该失败尝试额外消耗 `1494` tokens。
- 当前 consolidated candidate 已生成独立的 `audit.json`、`comparison.json` 和 `seal_readiness_audit.json`。

## 4. 当前判断

- Oracle `formal_v2` 仍是当前唯一可直接进入论文主表的正式结果。
- 真实 external-LLM Agent 已经从单-seed smoke 前进到 two-seed candidate，且成功 trace 全部是真实 `external_llm` 调用。
- two-seed candidate 的任务级指标与 `configured_agent_formal_v2` 的 `heuristic_fallback` Agent 在这 5 个 agent 场景上数值相同；两者主要差别是前者有真实 API 成本与显著更高的端到端延迟。
- two-seed candidate 仍不能写成论文最终封版结果，原因至少包括：
  - `seed_count=2 < 20`
  - 来源 run 混合了两个 git revision
  - `B6@1002` 存在一次已保留的失败首次尝试
  - 36 个场景上的真实 LLM Agent confirmatory 证据仍不存在

## 5. 结果目录

- `formal_v2` 聚合指标：`experiments/results/formal_v2/aggregated_metrics`
- `formal_v2` 报告：`experiments/results/formal_v2/reports`
- 真实 LLM two-seed candidate manifest：`experiments/results/agent_llm_smoke/reports/real_llm_candidate_20260725_two_seed/Ours/agent/manifest.json`
- 真实 LLM two-seed candidate 审计：`experiments/results/agent_llm_smoke/reports/real_llm_candidate_20260725_two_seed/Ours/agent/audit.json`
- 真实 LLM two-seed candidate 比较摘要：`experiments/results/agent_llm_smoke/reports/real_llm_candidate_20260725_two_seed/Ours/agent/comparison.json`
- 真实 LLM two-seed candidate 封版就绪度：`experiments/results/agent_llm_smoke/reports/real_llm_candidate_20260725_two_seed/Ours/agent/seal_readiness_audit.json`
