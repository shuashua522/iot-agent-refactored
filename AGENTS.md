# 仓库代理协作说明

本文件用于约束在本仓库内执行任务的各类代理、自动化助手或代码智能体的行为。

## 一、修改代码前的检查要求

1. 在修改任何项目代码之前，必须先检查当前 Git 工作区状态。
2. 建议使用命令：`git status --short`。
3. 如果发现存在未提交的修改或未跟踪文件，必须先暂停，不得直接继续改动代码。
4. 此时需要先征求用户确认，询问是否需要先提交现有变更，再继续后续开发。
5. 在用户明确确认处理方式之前，不得擅自覆盖、混合或忽略现有未提交内容。

## 二、文档同步要求

1. 任何会影响项目行为、结构、使用方式、部署方式、测试方式或开发流程的代码变更，都应同步检查相关文档是否需要更新。
2. 需要同步维护的文档包括但不限于：
   - `README.md`
   - `docs/` 目录下的文档
   - 仓库根目录中的设计说明、开发文档、方案文档等
3. 如果代码更新涉及文档内容，文档修改应与代码修改保持一致，不得遗漏。

## 三、提交要求

1. 当代码变更涉及文档更新时，代码和文档应纳入同一组变更中统一提交。
2. 除非用户明确表示“不需要提交”或另有指定，否则在完成代码与文档同步后，应进行提交。
3. 提交前应确保提交内容能够准确反映本次变更范围，避免遗漏相关文档。
4. 提交内容要尽可能使用中文描述。

## 四、行为原则

1. 不得在发现未提交改动的情况下直接开始新的代码修改。
2. 不得在未经用户确认的情况下擅自处理已有变更。
3. 应优先保证代码、文档与提交记录三者一致。
