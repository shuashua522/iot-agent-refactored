# 测试环境需求草案

本文档用于定义面向智能家居控制场景的测试环境。每条测试环境描述一组可复现的设备上下文，包括：

- 测试环境分类
- 参与设备与实体
- 初始状态
- 用户请求
- 目标状态
- 预期系统反馈
- 验证方式
- 备注

本文档当前重点覆盖以下四类场景：

- 正常
- 网络出错（第一次请求修改设备时返回网络异常、第二次请求时正常）
- 假成功（用户发送请求后，设备实际状态未改变，但返回响应告诉用户已成功，用户之后可能通过设备周围的传感器来检测实际是否调用成功）
- 请求参数错误或实体选择错误（比如调用实体不支持的功能）

## 编写原则

1. 每条测试环境都必须同时描述“系统返回给用户的结果”和“设备真实世界的结果”。
2. 对于“假成功”场景，必须额外提供可观测的旁路传感器或外部证据，用于判断设备是否真的执行成功。
3. 对于“网络出错”场景，必须明确失败发生在第几次请求，以及后续重试是否成功。
4. 对于“参数错误/实体错误”场景，必须明确错误原因属于：
   - 参数缺失
   - 参数取值非法
   - 实体不存在
   - 实体存在但不支持该服务

## 推荐模板

后续新增测试环境时，建议统一使用以下模板：

```md
### TE-xx 测试环境名称

- 分类：
- 测试目标：
- 参与设备：
- 关键实体：
- 初始状态：
- 用户请求：
- 故障注入：
- 目标状态：
- 预期返回：
- 验证方式：
- 通过标准：
- 备注：
```

---

## 测试环境清单

### TE-001 客厅台灯正常打开

- 分类：正常
- 测试目标：验证系统能够正确选择灯实体并成功执行 `light.turn_on`
- 参与设备：
  - 客厅台灯
  - 客厅照度传感器
- 关键实体：
  - `light.philips_cn_1061200910_lite_s_2`
  - `sensor.xiaomi_cn_blt_3_1ftnm7360c800_pir1_illumination_p_2_1005` 或等价照度传感器
- 初始状态：
  - 台灯状态为 `off`
  - 台灯亮度为 `0` 或接近 `0`
  - 房间照度较低，例如 `< 20 lx`
- 用户请求：打开客厅台灯，并将亮度设置为 60%
- 故障注入：无
- 目标状态：
  - 灯实体状态变为 `on`
  - 亮度属性更新为目标值附近
  - 房间照度在短时间内明显上升
- 预期返回：
  - 接口返回成功
  - 返回结果中包含变更后的灯实体状态
- 验证方式：
  - 查询灯实体状态
  - 查询照度传感器状态
  - 必要时比对调用前后事件日志
- 通过标准：
  - 返回成功
  - 灯实体状态与目标状态一致
  - 旁路照度信号支持“灯已真正打开”的结论
- 备注：这是最基础的正常链路，建议作为回归测试基线

### TE-002 音箱正常执行播放指令

- 分类：正常
- 测试目标：验证系统能够对媒体设备执行有效控制，并返回正确响应
- 参与设备：
  - 智能音箱
- 关键实体：
  - `media_player.xiaomi_cn_701074704_l15a`
  - `notify.xiaomi_cn_701074704_l15a_execute_text_directive_a_7_4`
- 初始状态：
  - 音箱状态为 `idle`
  - 音量存在有效值
- 用户请求：让音箱播放音乐
- 故障注入：无
- 目标状态：
  - 音箱状态切换为 `playing`，或产生明确的播放行为
- 预期返回：
  - 请求返回成功
  - 若使用支持返回结果的服务，返回体包含明确 `service_response`
- 验证方式：
  - 查询 `media_player` 状态
  - 检查最近一次消息或媒体动作属性
- 通过标准：
  - 返回成功
  - 媒体实体状态更新为预期值
- 备注：可用于验证“播放音乐”“下一曲”“暂停”等一组媒体控制能力

### TE-003 智能插座首次请求网络异常，第二次请求成功

- 分类：网络出错
- 测试目标：验证系统在第一次控制失败时能正确反馈失败，在第二次请求时恢复正常
- 参与设备：
  - 智能插座
  - 被插座供电的台灯或小家电
- 关键实体：
  - `switch.cuco_cn_269067598_cp1_on_p_2_1`
- 初始状态：
  - 插座状态为 `off`
  - 负载设备未通电
- 用户请求：打开插座
- 故障注入：
  - 第一次调用 `switch.turn_on` 时模拟网络异常，例如超时、连接失败或网关临时不可达
  - 第二次调用相同请求时恢复正常
- 目标状态：
  - 第一次调用后，插座真实状态仍为 `off`
  - 第二次调用后，插座真实状态变为 `on`
- 预期返回：
  - 第一次返回网络异常，不应伪装成成功
  - 第二次返回成功
- 验证方式：
  - 分别检查两次调用返回结果
  - 在每次调用后查询插座实体状态
  - 如有条件，检查负载设备是否通电
- 通过标准：
  - 第一次请求失败且状态未变化
  - 第二次请求成功且状态完成切换
  - 系统不会把第一次失败误报为成功
- 备注：该场景用于验证重试策略、失败提示和状态一致性

### TE-004 网关配置首次提交网络异常，重试后生效

- 分类：网络出错
- 测试目标：验证配置类写操作在瞬时网络问题下的重试表现
- 参与设备：
  - 多模网关
- 关键实体：
  - `number.lumi_cn_551385025_mcn001_indicator_brightness_p_6_3`
- 初始状态：
  - 指示灯亮度为 `100`
- 用户请求：将网关指示灯亮度设置为 `30`
- 故障注入：
  - 第一次调用 `number.set_value` 返回网络异常
  - 第二次调用恢复正常
- 目标状态：
  - 第一次调用后亮度保持 `100`
  - 第二次调用后亮度变为 `30`
- 预期返回：
  - 第一次明确失败
  - 第二次明确成功
- 验证方式：
  - 调用后查询 number 实体状态
  - 检查是否存在重复写入、副作用或中间脏状态
- 通过标准：
  - 失败请求不改变状态
  - 成功请求后状态与目标值一致
- 备注：该场景比开关类动作更适合检查“重试是否幂等”

### TE-005 灯控制假成功，但真实未亮

- 分类：假成功
- 测试目标：验证系统在接口返回成功但设备实际未执行时，能否通过旁路传感器发现异常
- 参与设备：
  - 客厅主灯
  - 客厅照度传感器
  - 人体传感器或摄像头分析模块（可选）
- 关键实体：
  - 灯实体：建议使用 `light.philips_cn_1061200910_lite_s_2` 或等价灯实体
  - 照度传感器：`sensor.xiaomi_cn_blt_3_1ftnm7360c800_pir1_illumination_p_2_1005` 或专门布置的照度实体
- 初始状态：
  - 灯状态为 `off`
  - 环境照度较低
- 用户请求：打开客厅主灯
- 故障注入：
  - 控制接口返回成功
  - 但设备真实状态保持不变，灯没有真正点亮
- 目标状态：
  - “系统记录状态”可能显示已成功
  - “真实物理状态”仍为关闭
  - 旁路照度传感器不出现预期上升
- 预期返回：
  - 首次返回成功提示
  - 后续校验阶段应发现结果不一致
- 验证方式：
  - 查询灯实体状态
  - 查询照度传感器是否在设定时间窗口内提升
  - 对比“接口响应成功”与“现实世界证据”是否一致
- 通过标准：
  - 测试系统能够识别这是一次“假成功”
  - 不仅依赖服务返回值，还能结合传感器证据做二次确认
- 备注：这是评估智能体是否具备“执行后验证”能力的关键场景

### TE-006 插座关闭假成功，但门窗磁/功率证据不支持

- 分类：假成功
- 测试目标：验证对开关类设备的成功判定不能只依赖接口响应
- 参与设备：
  - 智能插座
  - 插座供电设备
  - 功率传感器或其他外部观测手段
- 关键实体：
  - `switch.cuco_cn_269067699_cp1_on_p_2_1`
  - 功率类旁路实体，如果当前样本中没有，可在后续 mock 数据中补充
- 初始状态：
  - 插座状态为 `on`
  - 负载设备处于工作中
- 用户请求：关闭插座
- 故障注入：
  - `switch.turn_off` 返回成功
  - 但插座真实仍保持通电
- 目标状态：
  - 平台记录可能显示 `off`
  - 真实负载功耗仍大于零，或设备仍继续工作
- 预期返回：
  - 接口表面成功
  - 外部验证应判定执行失败
- 验证方式：
  - 查询插座实体状态
  - 查询功率、声音、画面或其他外部信号
- 通过标准：
  - 系统最终结论为“调用结果存疑或失败”，而不是直接相信返回成功
- 备注：建议作为“成功后必须校验”的代表场景

### TE-007 文本参数格式错误

- 分类：请求参数错误或实体选择错误
- 测试目标：验证服务参数合法性校验是否正确
- 参与设备：
  - 多模网关
- 关键实体：
  - `text.lumi_cn_551385025_mcn001_effective_time_p_6_2`
- 初始状态：
  - 实体当前值为合法时间段字符串，例如 `21:00-09:00`
- 用户请求：将生效时间设置为 `bad`
- 故障注入：无，直接使用非法参数
- 目标状态：
  - 实体状态保持原值不变
- 预期返回：
  - 返回参数错误
  - 清楚说明格式不符合要求
- 验证方式：
  - 检查接口错误码与错误信息
  - 再次查询实体状态，确认未被污染
- 通过标准：
  - 请求被拒绝
  - 实体状态不发生变化
- 备注：当前项目已有类似校验，适合作为参数错误基线用例

### TE-008 枚举参数取值错误

- 分类：请求参数错误或实体选择错误
- 测试目标：验证枚举型实体对非法选项的处理
- 参与设备：
  - 多模网关
- 关键实体：
  - `select.lumi_cn_551385025_mcn001_status_p_6_1`
- 初始状态：
  - 当前状态为 `Open`
  - 合法选项仅包含 `Open` 和 `Close`
- 用户请求：将选项设置为 `HalfOpen`
- 故障注入：无，直接使用不支持的选项
- 目标状态：
  - 实体状态保持不变
- 预期返回：
  - 返回参数非法或选项非法
- 验证方式：
  - 检查错误返回
  - 再次查询实体状态
- 通过标准：
  - 系统明确指出选项无效
  - 实体状态不改变
- 备注：用于区分“实体存在但参数不合法”和“实体不存在”两种错误

### TE-009 实体存在，但调用了不支持的服务

- 分类：请求参数错误或实体选择错误
- 测试目标：验证系统能够识别“实体选对了，但服务选错了”的情况
- 参与设备：
  - 门窗传感器
- 关键实体：
  - `binary_sensor.isa_cn_blt_3_1md0u6qht0k00_dw2hl_contact_state_p_2_2`
- 初始状态：
  - 门窗磁状态为 `off` 或 `on`
- 用户请求：对该实体执行 `light.turn_on` 或其他不支持的主动控制服务
- 故障注入：无
- 目标状态：
  - 传感器状态保持不变
- 预期返回：
  - 返回实体不支持该服务，或服务/领域不匹配
- 验证方式：
  - 检查错误码与错误信息
  - 查询实体状态确认无变化
- 通过标准：
  - 系统不会把传感器当成可控灯具
  - 错误提示足够明确，便于上层智能体修正实体选择
- 备注：这是“实体选择错误”中最常见的一类

### TE-010 实体不存在或实体 ID 填写错误

- 分类：请求参数错误或实体选择错误
- 测试目标：验证系统能正确处理不存在的实体
- 参与设备：无特定设备
- 关键实体：
  - 错误示例：`light.not_exist`
- 初始状态：系统中不存在该实体
- 用户请求：打开 `light.not_exist`
- 故障注入：无
- 目标状态：
  - 系统状态不应新增脏数据
  - 不应误操作其他相似实体
- 预期返回：
  - 返回实体不存在或服务调用失败
- 验证方式：
  - 检查错误码
  - 检查系统中未产生意外状态变更
- 通过标准：
  - 请求被拒绝
  - 不影响任何已有设备
- 备注：该场景用于验证实体检索与容错边界

---

## 后续可补充的测试环境方向

- 同一请求命中多个候选实体，验证消歧能力
- 请求语义正确，但目标房间错误，验证空间定位能力
- 设备离线但平台缓存状态未及时更新
- 多设备联动场景下部分成功、部分失败
- 需要 `return_response=true` 的服务与普通服务混合调用
- 按钮类实体触发联动动作后，联动目标未按预期变化

## 当前版本结论

当前建议至少保留以上 10 条测试环境，其中：

- `TE-001`、`TE-002` 作为正常基线
- `TE-003`、`TE-004` 作为网络异常与重试验证
- `TE-005`、`TE-006` 作为假成功验证
- `TE-007` 到 `TE-010` 作为参数错误与实体选择错误验证

如果后续你希望，我可以继续把这份文档再收紧成更正式的“需求文档”风格，例如再补两列：

- `系统应对要求`
- `智能体失败判定标准`

这样会更适合直接拿去驱动测试集构造和自动评测。
