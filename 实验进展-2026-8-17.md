# SAE Memory System 实验进展汇报（2026-08-17）

## 一、结论摘要

截至 2026 年 8 月 17 日，当前可以用于投稿的实验已经形成两层冻结证据：

1. **v4 正式主实验**已经完成。Ours、B0-B5 共 7 个系统完成 behavioral `2100/2100`、longitudinal `210/210`、robustness `28/28` 个实验单元；严格审计和 trace 审计通过，共记录 `2968` 次真实 external-LLM 调用和 `18,861,299` tokens，无 canonical fallback 或 transport failure。
2. **v4.1 原始文本写入补充实验**已经完成。7 个系统、3 条轨迹、10 个 replicate 共 `210/210` 个单元，最终审计通过，共 `210` 次 canonical LLM 调用和 `1,767,752` tokens。
3. **v4.2 投稿前补充实验尚未达到入表门槛**。完整产品 runtime、8 项真实 LLM 机制消融、扩展 longitudinal 都完成了真实 pilot 和审计链建设，但没有同时通过预注册的 1-replicate strict gate，因此未扩展到 10 replicates，也不进入论文统计表。

当前最稳妥的论文结论是：**在冻结的 experiments MemoryService + plan-only Agent adapter 和 v4 behavioral workload 范围内，Ours 相比 B0、B1、B4 有稳定且显著的 TSR 优势；对 B2 的证据不完全一致，对 B3、B5 没有显著优势，不能声称全面优于所有强基线。**

同时，v4.1 给出了重要的负结果：Ours 能稳定完成明确偏好创建和自然语言纠正，但跨实体偏好选择为 `0/10`，在该窄范围 ingestion workload 上低于 B1 和 B4。因此当前结果可以写进论文，但必须如实限定系统范围、统计证据和失败模式。

## 二、证据层级与投稿状态

| 证据层 | 规模 | 审计状态 | 投稿定位 | 当前结论 |
|---|---:|---|---|---|
| v4 behavioral | 7 系统 × 10 场景 × 30 replicates = `2100` | 通过 | 论文主表 | 当前最主要的确认性证据 |
| v4 longitudinal | 7 系统 × 1 轨迹 × 30 replicates = `210` | 通过 | 补充材料 | 只支持单条冻结轨迹，不支持广泛长期泛化 |
| v4 robustness | 7 系统 × 2 场景 × 2 replicates = `28` | 通过 | 补充材料/限制 | 只提供有限 held-out 证据 |
| v4.1 raw-text ingestion | 7 系统 × 3 轨迹 × 10 replicates = `210` | 通过 | 补充表/preliminary | 暴露跨实体写入与选择缺口 |
| v4.2 产品 runtime | 3 条真实自然语言轨迹 pilot | 未通过完整 gate | 不入表 | 工程链可审计，但端到端成功率未验证 |
| v4.2 真实 LLM 消融 | Ours + 8 消融，`16/16` pilot traces | activation/isolation 未全通过 | 不入表 | 不能报告机制贡献和效应量 |
| v4.2 扩展 longitudinal | 7 系统 × 5 轨迹 × 1 replicate = `35` | strict 未通过 | 不入表 | 只能作为工程诊断 |

这里的 `replicate_id` 表示同一系统和场景的独立重复运行编号，用于估计真实 LLM 输出波动；由于供应商不稳定支持 request-level seed，不能把它解释成模型端可控的随机种子。

## 三、v4 正式主实验结果

### 3.1 实验规模与可信度

- 冻结 revision：`2391012eed2b4e9670a520ee8c332d9be1145ff2`。
- Behavioral：`2100/2100`；Longitudinal：`210/210`；Robustness：`28/28`。
- Canonical external-LLM 调用：`2968` 次。
- Canonical token：`18,861,299`。
- strict audit、trace audit 均通过；canonical 数据中无 fallback、无 transport failure。
- 结果状态：`formal_results_ready_with_llm_assisted_annotation_deviation`。
- 验证范围：`experiments MemoryService + plan-only Agent adapter`，不是完整 `smartHome/m_agent` 产品 runtime。

### 3.2 Behavioral 主结果

| 系统 | TSR | Final-State TSR | Query Acc. | Automation Acc. | WDR | Unsafe Action Rate |
|---|---:|---:|---:|---:|---:|---:|
| **Ours** | **77.00%** | **100.00%** | **100.00%** | 80.83% | **0.00%** | **0.00%** |
| B0 | 31.00% | 0.00% | 0.00% | 90.83% | 73.33% | 0.00% |
| B1 | 28.33% | 11.11% | 0.00% | 38.33% | 88.89% | 3.33% |
| B2 | 69.00% | 97.78% | 100.00% | 64.17% | 2.22% | 0.00% |
| B3 | 74.67% | 100.00% | 100.00% | 84.17% | 0.00% | 0.00% |
| B4 | 30.67% | 8.89% | 0.00% | 56.67% | 91.11% | 10.00% |
| B5 | 71.00% | 100.00% | 100.00% | 69.17% | 0.00% | 3.33% |

Ours 的必要澄清率为 `60.00%`、不必要澄清率为 `0.00%`；平均 prompt tokens 为 `7885.5`，平均 completion tokens 为 `69.7`，平均端到端延迟约 `4588.1 ms`。

### 3.3 配对统计与正确解释

| 对比 | Ours - baseline TSR | Two-way clustered bootstrap 95% CI | Holm p | 应采用的解释 |
|---|---:|---:|---:|---|
| Ours vs B0 | +46.00 pp | [14.33, 76.34] pp | `1.19e-28` | 稳定优势 |
| Ours vs B1 | +48.67 pp | [22.33, 73.34] pp | `1.34e-37` | 稳定优势 |
| Ours vs B2 | +8.00 pp | [-1.33, 19.67] pp | `0.00911` | p 显著但 clustered CI 跨 0，证据混合 |
| Ours vs B3 | +2.33 pp | [-6.00, 12.33] pp | `1` | 无显著优势 |
| Ours vs B4 | +46.33 pp | [20.00, 71.33] pp | `1.40e-35` | 稳定优势 |
| Ours vs B5 | +6.00 pp | [-2.00, 16.33] pp | `0.123` | 无显著优势 |

因此，不能只根据均值写“Ours 优于所有 baseline”。论文中应把 B0、B1、B4 作为有稳健统计支持的优势，将 B2 写成敏感性检验与聚类置信区间不一致，将 B3、B5 写成性能接近且差异不显著。

### 3.4 Guard 诊断

Behavioral 共审计 `2520` 个 decision：raw planner accuracy 为 `56.90%`，guarded accuracy 为 `58.17%`，override rate 为 `6.27%`。`158` 次 override 中，corrected/harmful/neutral/unresolved 分别为 `59/27/72/0`。

这说明 guard 整体上有净正收益，但不是无条件安全：仍有 `27` 次 harmful override。论文可写“guard 平均改善决策准确率”，不能写“guard 总能纠正错误”或“不会产生有害覆盖”。

### 3.5 Longitudinal 与 Robustness

Longitudinal 只包含一条冻结轨迹，每个系统 30 次重复：Ours TSR=`100%`，B0=`80%`，B1-B5 均为 `100%`，Ours WDR=`0`。该结果说明该条跨 session 轨迹可以稳定完成，但由于只有一条轨迹，无法证明系统对过期记忆、多实体污染、容量压力和不同历史长度均具有广泛长期泛化能力。

Robustness 只有 2 个场景、每系统 2 次重复：Ours/B2/B3/B5=`100%`，B1=`50%`，B0/B4=`25%`。样本量过小，只能作为有限 held-out 行为证据，不能声称跨家庭、跨环境或跨模型的普遍泛化。

### 3.6 标注状态

当前 13 个场景采用两个独立模型标注，并由第三模型完成分歧裁决：model-model Cohen's kappa=`0.5455`。该状态为 `complete_llm_assisted`，`human_annotation_complete=false`，human kappa 仍为 `null`。

因此汇报和论文必须称为“独立模型辅助标注及第三模型裁决”，不能称为“真实双人人工标注”。这不会抹去已完成的实验结果，但属于投稿时需要主动披露的方法学限制。

## 四、v4.1 原始文本写入补充实验

### 4.1 协议与审计

- 冻结 revision：`48cc3b49ffd0c759791710ce06fab1435178a17b`。
- 矩阵：Ours+B0-B5、I1-I3 三条轨迹、replicate `1001-1010`，共 `210/210` unit。
- Final audit=`pass`；无 heuristic fallback、无 usage 缺失、无 runtime hidden bridge。
- B1 保持 top-k raw-text RAG；B4 保持 full raw history；baseline fidelity 通过。
- Canonical API 调用 `210` 次，canonical tokens=`1,767,752`；共记录 `219` 次 transport attempts 和 `1` 个 repaired unit。
- 工作负载仅覆盖规则型温度偏好创建、纠正和跨实体选择，不是通用 LLM extraction，也不是完整产品 runtime。

### 4.2 结果

| 系统 | 成功数 | TSR | Total tokens | I1 / I2 / I3 成功数 |
|---|---:|---:|---:|---:|
| **Ours** | 20/30 | **66.67%** | 264,399 | 10/10 / 10/10 / 0/10 |
| B0 | 0/30 | 0.00% | 264,394 | 0/10 / 0/10 / 0/10 |
| B1 raw-text RAG | 27/30 | **90.00%** | 245,635 | 10/10 / 10/10 / 7/10 |
| B2 | 19/30 | 63.33% | 238,048 | 10/10 / 9/10 / 0/10 |
| B3 | 20/30 | 66.67% | 234,702 | 10/10 / 10/10 / 0/10 |
| B4 full raw history | 25/30 | **83.33%** | 281,659 | 9/10 / 10/10 / 6/10 |
| B5 | 20/30 | 66.67% | 238,915 | 10/10 / 10/10 / 0/10 |

I1 是明确偏好创建，I2 是自然语言纠正，I3 是跨实体偏好选择。Ours 在 I1/I2 上均为 `10/10`，证明明确写入与纠正链较稳定；但 I3 为 `0/10`，说明当前 entity selection/ingestion 路径不能可靠地从多个实体偏好中选出目标实体。B1 和 B4 在 I3 上分别达到 `7/10`、`6/10`，并使总 TSR 高于 Ours。

这项结果不推翻 v4 behavioral 主实验，因为两个 workload 的输入边界与研究问题不同；但它限制了论文对“原始自然语言记忆写入能力”的表述。应把该结果作为诚实的补充实验和后续改进方向，而不是隐藏负结果。

## 五、v4.2 已完成内容及未入表原因

### 5.1 完整产品 Runtime

产品验证确实调用了 `smartHome.m_agent.agent.base_home_agent.run_ourAgent` 和 `smartHome.m_agent.memory.runtime_v1.DemoMemoryRuntime`，不是 experiments adapter。已完成以下工程工作：

- 修复 `try.memory` 的 SQLite FTS 字面查询兼容问题，`no such column: code` 未再复发。
- 每条轨迹使用独立子进程和独立 SQLite，失败时仍保存 canonical JSON、退出码和 stdout/stderr。
- 增加 task-scoped 真实 usage、provider/model、tool-call 和 transport-attempt telemetry。
- 实现仅针对已识别 transport 签名、每个模型请求最多一次的受限 repair，避免重放工具或整条任务。
- 分离验证 `newapi/gpt-5.4-mini` 与 `my_gac/gpt-5.4`，不混合 provider 结果。

部分 pilot 中 R1/R2 曾成功，完整路径也真实进入 router、filter、`query_tool`、SQLite 持久化和设备工具；但三条预注册轨迹从未在同一个冻结 gate 中全部成功。`newapi` 出现间歇 400、timeout 和 channel failure。因此只能说“完整产品 runtime 已完成工程级可审计接入”，不能报告可复现的产品 runtime TSR。

### 5.2 真实 LLM 机制消融

Ours 与 8 个消融系统完成了 8 条预注册自然语言轨迹、1 replicate、`16/16` traces，且均为 external LLM、保留 usage 和 transport attempt、无 heuristic fallback。但 activation/isolation 未全部通过：部分轨迹没有先建立后续 revise/feedback/merge/ripple/split 所需的前置记忆，导致目标机制未实际激活。

在禁止场景 patch、禁止 runner 直接补写 memory operation 的约束下，这些空操作不能被改写为机制激活。因此当前不能计算或报告 Ours-minus-ablation effect size、置信区间或独立机制贡献。需要修改并重新预注册自然语言轨迹后，才能重新开始 1-replicate gate。

### 5.3 扩展 Longitudinal

扩展 pilot 已覆盖 5 条轨迹和 7 个系统，共 `35/35` traces，包含跨 session 更正、过期记忆、多实体污染、20/40/80 history growth、容量压力或长期冲突；B1/B4 fidelity 通过。

首轮有 3 个 canonical timeout，repair 后第二轮仍有 2 个单元连续 timeout 且 canonical usage 为 0，strict audit 因而失败。首轮观察到的 Ours `2/5`、B0 `0/5`、B1 `2/5`、B2 `3/5`、B3 `3/5`、B4 `4/5`、B5 `2/5` 仅用于诊断，不是论文统计结果。L5 中 B4 的 full-history token 成本明显高于 Ours，可作为行为观察，但不能替代预注册规模的统计检验。

## 六、目前可以写入论文的内容

1. v4 behavioral 主表及配对统计，可以作为当前论文主实验。
2. Ours 相对 B0、B1、B4 的稳定优势，以及对 B2/B3/B5 的有限或不显著结果。
3. Ours 的 Final-State TSR、Query Accuracy、WDR、Unsafe Action Rate、澄清率、真实 token 和延迟指标。
4. Guard 的平均净收益及 27 次 harmful override，作为完整诊断而不是单向正面描述。
5. 单轨迹 longitudinal 和小规模 robustness，放入补充材料并明确样本与外推限制。
6. v4.1 ingestion 的全部结果，包括 Ours 的 I1/I2 成功和 I3 失败，以及 B1/B4 在该 workload 上优于 Ours。
7. 真实调用、失败尝试保留、strict/trace/fidelity/SHA-256 审计和冻结 revision，作为可复现性证据。

## 七、目前不能声称的内容

1. 不能声称 Ours 在所有指标或所有 baseline 上均显著最好。
2. 不能声称已经验证完整 `smartHome/m_agent` 产品 runtime 的任务成功率。
3. 不能把 v4 experiments adapter 结果写成完整产品 Agent 结果。
4. 不能声称 8 项机制均已通过真实 LLM 消融证明独立贡献。
5. 不能根据一条 longitudinal 轨迹声称广泛长期记忆泛化。
6. 不能根据 2×2 robustness 子集声称跨环境或跨模型泛化。
7. 不能称当前标注为真人双人独立标注或报告人工 Cohen's kappa。
8. 不能声称 Ours 的通用 raw-text ingestion 优于 B1/B4。

## 八、投稿前主要风险与建议

| 优先级 | 风险 | 对论文的影响 | 当前建议 |
|---|---|---|---|
| P0 | 主结论表述过强 | 容易被统计结果直接反驳 | 只主张 B0/B1/B4 的稳健优势，完整报告 B2/B3/B5 |
| P0 | experiments adapter 与产品 runtime 混称 | 构成验证范围错误 | 方法和限制中明确 claim scope |
| P0 | 模型标注被误写成人工标注 | 构成标注流程错误 | 保留 `complete_llm_assisted` 和 `human_annotation_complete=false` |
| P1 | v4.1 的 I3 负结果 | 限制通用写入能力主张 | 作为补充表完整报告，并定位 entity selection 缺口 |
| P1 | longitudinal/robustness 覆盖有限 | 泛化结论较弱 | 降低措辞，不临时混入未过 gate 的 v4.2 pilot |
| P2 | v4.2 provider 不稳定 | 无法形成完整产品实验统计 | 投稿后或 API 稳定后以新 revision/new root 重跑 |
| P2 | 真实 LLM 消融轨迹未激活机制 | 机制贡献证据不足 | 修改协议并重新预注册，不能修补当前结果 |

如果当前时间只用于汇报和投稿整理，优先工作不是继续扩大 API 调用，而是统一论文表格、方法、统计解释和限制章节，使所有数字都指向 v4/v4.1 冻结工件，并主动披露 v4.1 负结果和 v4.2 未完成状态。

## 九、实验结果所在路径（以 `iot-agent-refactored_demo` 为根路径）

### 9.1 v4 正式结果

- 结果根：`experiments/results/protocol_v4_formal_bundle_llm_assisted_20260815/`
- 可读总报告：`experiments/results/protocol_v4_formal_bundle_llm_assisted_20260815/formal_results_summary.md`
- 机器可读总报告：`experiments/results/protocol_v4_formal_bundle_llm_assisted_20260815/formal_results_summary.json`
- 工件清单：`experiments/results/protocol_v4_formal_bundle_llm_assisted_20260815/artifact_manifest.json`
- Behavioral 系统指标：`experiments/results/protocol_v4_formal_bundle_llm_assisted_20260815/behavioral_system_metrics.csv`
- Longitudinal 系统指标：`experiments/results/protocol_v4_formal_bundle_llm_assisted_20260815/longitudinal_system_metrics.csv`
- Robustness 系统指标：`experiments/results/protocol_v4_formal_bundle_llm_assisted_20260815/robustness_system_metrics.csv`

### 9.2 v4.1 原始文本写入补充结果

- 结果根：`experiments/results/protocol_v4_1_supplemental_48cc3b4_20260816/`
- Freeze manifest：`experiments/results/protocol_v4_1_supplemental_48cc3b4_20260816/freeze_manifest.json`
- 最终分析：`experiments/results/protocol_v4_1_supplemental_48cc3b4_20260816/reports/final_analysis.json`
- 最终审计：`experiments/results/protocol_v4_1_supplemental_48cc3b4_20260816/audits/final_audit.json`
- 单元 trace：`experiments/results/protocol_v4_1_supplemental_48cc3b4_20260816/units/`

### 9.3 v4.2 诊断与状态说明

- 权威状态报告：`docs/v4.2补充实验状态报告-2026-08-16.md`
- 机制 pilot：`experiments/results/protocol_v4_2_supplemental_e916a42_20260816/`
- 扩展 longitudinal pilot：`experiments/results/protocol_v4_2_longitudinal_e916a42_20260816/`
- 产品 runtime 的各次失败/恢复根路径详见 v4.2 状态报告；这些目录不是论文结果表来源。

### 9.4 汇报与协议文档

- 当前权威汇报：`docs/v4最终实验与v4_1补充实验汇报-2026-08-16.md`
- 当前结果摘要：`docs/实验结果摘要.md`
- v4.2 状态：`docs/v4.2补充实验状态报告-2026-08-16.md`
- 实验方案：`docs/实验方案-v3(1)-中文翻译.md`
- 工作记录：`docs/WORKLOG.md`

## 十、汇报短版

目前论文实验主线已经完成并冻结。v4 正式实验覆盖 Ours 与 B0-B5 共 7 个系统，behavioral 为 10 个场景 × 30 次重复，共 2100 个单元；另有 210 个 longitudinal 单元和 28 个 robustness 单元，strict/trace 审计均通过。全部 v4 结果共记录 2968 次真实 LLM 调用、约 1886 万 tokens，无 canonical fallback。Behavioral 中 Ours 的 TSR 为 77.00%，B0-B5 分别为 31.00%、28.33%、69.00%、74.67%、30.67%、71.00%。Ours 相对 B0、B1、B4 的优势有 clustered CI 和 Holm 校正后的稳健支持；对 B2 的统计证据不一致，对 B3、B5 没有显著优势，所以论文不会写成全面优于所有基线。补充完成的 v4.1 原始文本写入实验共 210 个单元，Ours 在明确偏好创建和自然语言纠正上均为 10/10，但跨实体选择为 0/10，总 TSR 66.67%，低于 B1 的 90.00% 和 B4 的 83.33%，这暴露了 entity selection 的真实缺口。完整产品 runtime、真实 LLM 机制消融和扩展长期实验已经完成工程 pilot，但因 provider 不稳定或机制未实际激活，未通过预注册 strict gate，故不混入论文结果。当前主结果已经可用于投稿，但会明确限定为 experiments MemoryService + plan-only Agent adapter，并披露模型辅助标注、长期覆盖有限和完整产品 runtime 尚未形成可复现实验统计等限制。

## 十一、与 2026-07-26 旧报告的关系

`实验进展-2026-7-26.md` 中的 Oracle `formal_v2` 和旧 36 场景、30 seed、`7560` unit、Ours TSR=`88.80%` 结果属于早期协议。旧协议在 composite TSR、evaluator truth 隔离、gold `memory_ops`/`action_template`、baseline fidelity 和 claim scope 等方面与当前 v4 不同。

这些旧结果可以作为研发历程和诊断证据保留，但**不再是当前投稿的权威主结果，也不能与 v4/v4.1 合并统计**。当前论文与汇报应以本文件列出的 v4 正式结果和 v4.1 补充结果为准。
