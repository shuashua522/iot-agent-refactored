domain: input_select
service: select_next
name: Next
description: Selects the next option.
fields:
  cycle:
    required: false
    name: Cycle
    description: If the option should cycle from the last to the first option on the
      list.
    selector:
      boolean: {}
target:
  entity:
  - domain:
    - input_select
handler: builtin:service.not_implemented
supports_response: false
