domain: timer
service: cancel
name: Cancel
description: Resets a timer's duration to the last known initial value without firing
  the timer finished event.
fields: {}
target:
  entity:
  - domain:
    - timer
handler: builtin:service.not_implemented
supports_response: false
